<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SejenakApps">

        <!-- Login sebagai halaman pertama -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
           <intent-filter>
               <action android:name="android.intent.action.MAIN" />
               <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register -->
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- Tambahkan MainActivity -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- Legacy & Sejenak -->
        <activity android:name=".LegacyMainActivity" android:exported="false" />
        <activity android:name=".view.SejenakActivity" android:exported="true" />

        <!-- Quiz & Result -->
        <activity android:name=".QuizActivity" android:exported="false" />
        <activity android:name=".ResultActivity" android:exported="false" />

        <activity android:name=".InformasiActivity" android:exported="false" />
        <activity android:name=".DetailPsikologActivity" android:exported="false" />


    </application>
</manifest>
